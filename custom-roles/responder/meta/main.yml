---
dependencies: []

galaxy_info:
  role_name: responder
  author: David-B-Moore
  description: An Ansible Role that configures a scheduled task to reach out to a nonexistent machine name. This makes the machine vulnerable to Responder. 
  company: Cedarville University
  license: GPLv3
  min_ansible_version: "2.10"
  platforms:
    # Modify the platforms here as required
    # This example includes a lot, see https://github.com/ansible/ansible-lint/blob/main/src/ansiblelint/schemas/meta.json for all possibilities
    - name: Windows 
      versions:
        - "2012R2"
        - "2016"
        - "2019"
        - "2022"
  galaxy_tags:
    - tagswithnospaces

collections: [] # Add any collections needed here
