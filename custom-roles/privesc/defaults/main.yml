---
# Role variables should all be prefaced with the name of the role to avoid
# name conflicts with other roles

# The name of the binary to configure for privilege escalation
ludus_privesc_binary: ""
ludus_privesc_package: "{{ ludus_privesc_binary }}"
