Note: this role is deprecated. It was essentially my first-draft version of trying
to write an Ansible role to set up websites, before I realized I need to be able 
to handle websites that run on Node JS as well.

This role works, but the other web-setup role likely works better, so use that.
