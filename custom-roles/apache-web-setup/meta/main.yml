---
dependencies: []

galaxy_info:
  role_name: ludus_apache_web_setup
  author: David-B-Moore
  description: An Ansible Role that installs Apache and sets up a website by
    pulling a zip file from a specified git repo and unzipping it into /var/www/html
  company: Cedarville University
  license: GPLv3
  min_ansible_version: "2.10"
  platforms:
    # Modify the platforms here as required
    # This example includes a lot, see https://github.com/ansible/ansible-lint/blob/main/src/ansiblelint/schemas/meta.json for all possibilities
    - name: Debian
      versions:
        - buster
        - bullseye
        - bookworm
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy
  galaxy_tags:
    - tagswithnospaces

collections: [] # Add any collections needed here
